<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ml.p_seminar.apoutdoortools">

    <uses-permission android:name="android.permission.INTERNET" />
    //Die hier aufgelisteten Zugriffsrechte werden für die Standortermittlung durch das GPS benöigt
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    //Die hier aufgelisteten Zugriffsrechte werden für die Standortermittlung durch das GPS benöigt
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    //Die hier aufgelisteten Zugriffsrechte werden für die Standortermittlung durch das GPS benöigt
    <uses-permission android:name="android.permission.VIBRATE" />
    //Benötigt um auf den Vibrationsmotor zugreifen zu können
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".handbuch"
            android:label="@string/title_activity_handbuch"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".handbuchFragement"></activity>
    </application>

</manifest>